# Stack context

- Runtime: Node.js 20 or newer, ECMAScript modules.
- Dependencies: Node.js standard library only at runtime.
- CLI entry: `bin/cli.js`.
- Tests: built-in `node:test` under `test/`.
- Packaging: npm package `create-ai-native-sdlc`.
- Required checks: `npm test` and `npm pack --dry-run`.
