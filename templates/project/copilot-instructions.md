# {{PROJECT_NAME}} AI-native Workflow

Treat `ai-native.yaml` as the source of truth. Read the matching role under `.github/agents/` and the project baselines under `.ai-native/baseline/` before working.

## Roles

{{ROLE_LIST}}

## Workflow

{{WORKFLOW_LIST}}
