version: 1

resources:
  role: {{PM_BA_ROLE_PATH}}

# Add project-relative feature briefs, business notes, or interview notes here.
inputs:
  markdown: []

# The output root and artifact paths come from ai-native.yaml.
# This role may only choose their shared child directory.
output:
  subdirectory: ai-native/product
