# Delivery artifact index

Use this file as the single entry point for delivery documents owned by this repository. Only list artifacts that exist and can be opened.

For an artifact owned by another repository, use `.ai-sdlc/artifact-hosts.json` and the `sdlc-artifact-bridge` skill to find the canonical source. For technology guidance, resolve the catalog first and then the child profile it names. Do not copy a remote artifact into this index merely to make it local.

| Artifact | Description |
|---|---|
